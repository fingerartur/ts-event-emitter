{
  "name": "@fingerartur/ts-event-emitter",
  "version": "1.0.3",
  "description": "Event emitter for typescript",
  "author": "Artur Finger",
  "license": "ISC",
  "keywords": [ "event", "emitter" ],
  "repository": "https://github.com/fingerartur/ts-event-emitter",
  "main": "dist/index.js",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "rm -rf dist && webpack --config=./webpack.config.js",
    "test": "jest --config=./jest.config.js",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "jest": "^26.0.0",
    "ts-jest": "^26.2.0",
    "ts-loader": "^8.0.4",
    "typescript": "^4.0.2",
    "webpack": "^5.32.0",
    "webpack-cli": "^3.3.12",
    "webpack-merge": "^5.2.0"
  },
  "files": [
    "dist"
  ]
}
